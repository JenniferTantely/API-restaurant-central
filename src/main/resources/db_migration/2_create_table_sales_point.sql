CREATE TABLE sales_point (
                             id SERIAL PRIMARY KEY,
                             name VARCHAR(255) NOT NULL,
                             url TEXT,
                             api_key TEXT
);
